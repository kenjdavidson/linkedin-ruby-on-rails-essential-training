<h2>Welcome back, <%= "#{@username} (#{@user_id})" %></h2>
<ul>
    <li><%= link_to('Manage Subjects', subjects_path) %></li>
    <li><%= link_to('Logout', logout_path, method: :delete) %></li>
</ul>
